I think we need to frame the project objective from bank to a bureau like the cfpb.
1- i downloaded the all records with narratives in csv
2- changed to parquet for better memory management
3- then removed all columns except the narrative and product column. 
4- then merged product catgories to get clean 10 classes, in which the 10th class involves those 
    labeled as other(unspecified) complaints.
5- then checked if there is inherent difference in complaint length among the various departments
    found that complaint text length is not a problem
6- then given a complaint id column for the cleaned data
7- splited the data into training and testing data.
8- trained a basic NB model and evaluated to see if we can proceed with the dataset
9- found that the Financial service department lacks distinct vocabulary. so it will 
    affect model score. it is data labeling problem. so removed that category and left with 9 departments.
10- saved the cleaned data
11- trained LR model. saw improvement over NB model
12- trained SVM mode. again saw minor imporovement over LR model
13- feature engineered the priority class (immediate, same-day, regular)